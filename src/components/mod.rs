//! src/components/mod.rs — Built-in component registry
//!
//! First-class engine components: the engine expects these and its systems interface with them (e.g. NavMeshAgent <-> baked navmesh), exactly like Unity's built-in components.
//!
//! Allowed deps: components::*.
//! Status: SCAFFOLD — structure only; not yet implemented.
