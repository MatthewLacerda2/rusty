# Editor cards grandfathered past the editor↔shared-op parity gate (#287).
# Format: one component-field name per line (`#` comments + blank lines ignored).
#
# These are first-class components whose inspector card still mutates the component
# DIRECTLY (`&mut entity.<field>` + egui widgets bound to its fields), instead of
# routing every write through a shared `scene::authoring` op the way the Lua API does.
# That direct mutation is the drift this gate exists to stop: the editor and the Lua
# binding can silently diverge.
#
# Burn-down only (#287). When you migrate a card to route through a shared op (the
# `material` card is the first — note it is ABSENT here), REMOVE its line; a routed
# card left on this list fails the gate as a stale entry. Never add a new line: a new
# direct card is fresh drift to fix, not to grandfather (baseline only, exceptionally,
# with written justification).
animator
audio
camera
collider
light
nav_agent
particles
rigidbody
visual_correction
