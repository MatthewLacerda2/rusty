# Components grandfathered past the 4-axis completeness gate (#81).
# Format: `<component_field> <axis>` (axes: add_menu, inspector, api).
# Burn-down only (#82) — remove a line when the axis is closed; never add one.
#
# EMPTY as of #82: every grandfathered gap is now either CLOSED (animator got its
# own Add Component entry; `light` got a real `src/api/light.rs` namespace) or a
# documented WAIVE in the gate itself (`WAIVERS` in tools/lint/src/components.rs)
# for axes deliberately served by a shared namespace (texture->Material,
# collider/rigidbody->Physics, nav_agent->NavMeshAgent, visual_correction->Graphics)
# or a content-driven workflow (mesh add_menu / api). The stale `script api` line
# was dropped — `scripts` is a `Vec`, never discovered by the gate.
#
# This is the burn-down list for axes we still intend to IMPLEMENT; it is empty.
# Decisions to NOT implement an axis live in `WAIVERS`, where each carries its
# written justification (auditable in git), so they are never silent skips.
